syntax = "proto3";

package hello;
option go_package = ".;hello";

// 定义问候服务
service Greeter {
  // 定义一个简单的RPC方法
  rpc SayHello (HelloRequest) returns (HelloReply) {}
}

// 请求消息，包含一个字符串
message HelloRequest {
  string name = 1;
}

// 响应消息，包含一个字符串
message HelloReply {
  string message = 1;
}